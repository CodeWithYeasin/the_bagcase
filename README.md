# the_bagcase

Ekta is a simple 3D e-commerce landing website for a bag business.

## Run

From the repository directory, start a local server:

```bash
python3 -m http.server 8000
```

Then open:

`http://127.0.0.1:8000/index.html`
